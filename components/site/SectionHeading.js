/**
 * SectionHeading is the original name for this component; SectionMarker.js
 * is now where it lives, with a `hue` prop added for intent-scoped sections.
 * Re-exported here so every existing call site — home page, business page,
 * tools pages, glossary, resources, news, destinations grid — keeps working
 * without a mechanical rename pass across the whole site.
 */
export { default } from "@/components/site/SectionMarker";
